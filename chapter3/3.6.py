import math

code = eval(input("Enter an ASCII coded: "))
#character = ord(code)
#area = (n * math.pow(s, 2)) / (4 * math.tan(math.pi / n))

# Print the equivalent character of the given ASCII code
print("The character is", chr(code))
